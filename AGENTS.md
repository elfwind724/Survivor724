# 项目目标

这是一个 Three.js 3D 俯视生存游戏。
核心是幸存者持续模拟、基地建设、白天工作、玩家副本探索和夜间尸潮指挥。

本地仓库在 ORICO 移动硬盘：`/Volumes/ORICO/dawn-bastion`。
远程仓库是 `https://github.com/elfwind724/Survivor724`。
开始工作前确认 `/Volumes/ORICO` 已挂载。不要把工作副本挪到 Mac 主盘。

# 不可违反的玩法原则

1. NPC 外出工作时必须真实存在于世界中，不得使用派遣后随机返回资源的机制。
2. 玩家进入副本后，基地和外部 NPC 仍继续低频模拟。
3. 夜间 NPC 负责自主战斗，玩家负责防区、增援、撤退和集火指令。
4. 第一人称只能接管现有幸存者，不创建独立角色或独立战斗数据。
5. 资源必须存在于容器、背包、地面或仓库中，不得无来源地进入全局数字。
6. 建筑必须经历蓝图、材料搬运和施工过程。
7. 不得未经批准增加新的大型系统。

# 技术原则

1. 模拟层与 Three.js 渲染层分离。
2. Mesh 不得保存核心游戏规则。
3. 使用固定时间步更新模拟。
4. HUD 和复杂菜单使用 DOM。
5. 重复敌人优先使用 InstancedMesh 和对象池。
6. 普通枪械优先使用射线命中。
7. 所有系统必须保持 TypeScript 严格类型。
8. 每个阶段完成后必须执行类型检查和生产构建。

# 目录边界

- `src/`：当前正式代码。只允许写这里。
- `docs/`：当前有效规则。`docs/01`–`docs/11` 优先于 `docs/source/`。
- `reference/`：只读参考。禁止把旧架构复制进 `src/`。只有经过评估、确认适合当前分层的模块才能迁移。
- `archive/`：已废弃内容。禁止采用。
- 不要参考、不要继续修改 `/Volumes/ORICO/Codex-deepseek/survival-highway`。
- 不要混入 Godot《永远的避难所》、Phaser 公路原型或其他旧仓。

# 当前开发阶段

当前只允许开发 GDD **M0**：架构骨架和 NPC 工作闭环。

口头说的「M1 NPC 工作闭环」等于本仓库的 **M0**。GDD 的 M1 是基地路径与建造，现在禁止开始。

不得提前制作完整尸潮、完整副本、第一人称战斗或大量美术资产。
`@dimforge/rapier3d-compat` 已锁定版本，但 M0 不得 import 或使用。

# 每次工作纪律

1. 先阅读 `AGENTS.md`、`docs/00-source-and-precedence.md` 和当前里程碑相关文档。
2. 先写验收测试与调试面板，再写玩法。
3. 不重写已经工作的系统。
4. 模拟状态不得存入 Mesh 或 DOM。
5. 不使用随机定时器伪装 NPC 工作结果。
6. 每个外勤任务必须包含路径、工具、背包、工作和返程。
7. 新功能完成后必须提供可复现的测试步骤。
8. 遇到范围外需求记录到 backlog，不提前实现。
9. 未通过当前里程碑验收，不进入下一阶段。

# 验证命令

```bash
npm run typecheck
npm run test
npm run build
```

# CLI 进入仓库后的第一件事

先只读，不立即改代码：

1. 完整阅读 `AGENTS.md` 和 `docs/`。
2. 检查 `package.json`、`src/` 与 `reference/`。
3. `reference/` 只作为技术参考，不代表当前项目架构。
4. 进入计划阶段，等待批准后再改代码。
